{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Site in VS Code Browser",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev -- --host localhost",
      "cwd": "${workspaceFolder}",
      "serverReadyAction": {
        "pattern": "Local:\\s+(https?://\\S+)",
        "uriFormat": "%s",
        "action": "openExternally"
      }
    }
  ]
}